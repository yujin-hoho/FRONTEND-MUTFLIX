name = "mutflix-stream"
main = "worker.js"
compatibility_date = "2024-01-01"

[vars]
# Allowed origins — bisa diisi comma-separated
# Contoh: "https://mutflix.onrender.com,https://melancholia112-mutflix.hf.space"
# Pakai * untuk izinkan semua (token sudah jadi auth layer)
ALLOWED_ORIGINS = "*"
